import {describe} from '@jest/globals';

describe('Test Consumer Kafka bath devices', () => {
    it('should fail when body is an empty object or undefined', () => {
        console.log('Teste');
    })
   
});
