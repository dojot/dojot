apiVersion: v1
kind: Service
metadata:
  labels:
    name: image-manager
  name: image-manager
  namespace: {{ dojot_namespace }}
spec:
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    name: image-manager
    app: dojot
