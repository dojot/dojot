apiVersion: v1
kind: Service
metadata:
  labels:
    name: flowbroker
  name: flowbroker
  namespace: {{ dojot_namespace }}
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: flowbroker
    app: dojot
